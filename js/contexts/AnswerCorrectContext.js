import React, { createContext, Component } from 'react';
import ReactDOM from "react-dom";

export const AnswerCorrectContext = createContext();

class AnswerCorrectContextProvider extends Component {
    render () {
        return null
    }
}

export default AnswerCorrectContextProvider;